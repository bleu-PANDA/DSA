#include <bits/stdc++.h>
using namespace std;
// Watermelon - Codeforces 4A
// Check if watermelon can be split into two even parts
int main(){
    int w; cin>>w;
    cout<<(w>2 && w%2==0 ? "YES" : "NO")<<"\n";
}
